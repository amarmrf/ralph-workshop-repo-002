# Ralph Artifacts

AFK scripts write one transcript per iteration into this directory.

These generated logs are ignored by git. Keep this directory committed so the scripts always have a stable target.
